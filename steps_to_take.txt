                  
rsync -rI \
    /System/Library/CoreServices/SystemAppearance.bundle/Contents/Resources/  ~/Desktop/saResourcesBackup
  
-- Launch terminal and enter commands below


1. mkdir ~/livemount

# find your disk with this command           
2. df / | tail -1 | awk '{print $1}' | sed 's/s[0-9]*$//'
# mine is /dev/disk2s5
 
# mount it          
3. sudo mount -o nobrowse -t apfs  /dev/disk1s5 ~/livemount
           
4. cd ~/livemount

# This command is for older macOS operating systems such as High Sierra           
6. sudo bless --folder ~/livemount/System/Library/CoreServices --bootefi --create-snapshot. 

# This is the updated command to bless the mounted folder
6A = sudo bless --mount ~/livemount --bootefi --create-snapshot

7. Reboot and that's that.
